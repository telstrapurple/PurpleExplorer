<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:PurpleExplorer.ViewModels"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="PurpleExplorer.Views.AddMessageWindow"
        Title="Send message" Width="700" Height="200">

    <Design.DataContext>
        <viewModels:AddMessageWindowViewModal />
    </Design.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <TextBox MinWidth="600" MinHeight="150" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                 TextWrapping="Wrap" Text="{Binding Path=Message, Mode=TwoWay}" />
        <Button Grid.Row="1" Width="200" Height="30" HorizontalAlignment="Right" VerticalAlignment="Center"
                IsEnabled="{Binding Message, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                Click="btnAddClick" Margin="0,0,5,0">
            Send
        </Button>
    </Grid>
</Window>